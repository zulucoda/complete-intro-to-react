/**
 * Created by muzikayise on 2016/10/23.
 */
/* eslint-env mocha */

const { expect } = require('chai')

describe('<Search />', () => {
  it('should pass', () => {
    expect(1 + 1 === 2).to.be.true
  })
})
